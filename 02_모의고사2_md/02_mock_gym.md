# 모의고사 — 헬스장 입장 시스템 (기본)

> 응용SW 기초 기술 활용 · 미들웨어 파트 | 코딩 문제 1문항 · **80점**

---

## 📋 상황 설명

당신은 **헬스장 입장 시스템**을 만들고 있습니다.
이 시스템에는 두 가지 핵심 부분이 있습니다.

| 구분 | 함수명 | 역할 |
|------|--------|------|
| 미들웨어 | `check_membership(membership)` | 입장 전, 회원권 유효 여부를 확인하는 기능 |
| 메인 시스템 | `enter_gym(name)` | 입장을 처리하는 기능 (이미 작성되어 있음) |

---

## 🎯 수행 과제

아래 조건에 맞게 파이썬 코드를 완성하세요.

**① `check_membership(membership)` 함수를 작성하세요.**

- 회원권 상태(`membership`)를 문자열로 입력받습니다.
- `"유효"` 이면 `"[회원권확인] 입장 가능합니다"` 를 출력하고 `True` 를 반환합니다.
- 그 외이면 `"[회원권확인] 유효한 회원권이 없습니다"` 를 출력하고 `False` 를 반환합니다.

**② main 코드를 작성하세요.**

- 회원권 상태를 `input()` 으로 입력받습니다.
- 미들웨어(`check_membership`)의 결과가 `True` 일 때만 이름을 입력받고 `enter_gym()` 이 실행됩니다.

---

## 💻 시작 코드

```python
def enter_gym(name):
    print(f"💪 {name}님, 입장을 환영합니다!")


# ↓ 여기서부터 작성하세요

def check_membership(membership):
    pass  # 이 부분을 완성하세요


if __name__ == "__main__":
    pass  # 이 부분을 완성하세요
```

---

## 📌 실행 예시

**예시 1 — 정상 입장**
```
회원권 상태를 입력하세요 (유효/만료): 유효
[회원권확인] 입장 가능합니다
이름을 입력하세요: 홍길동
💪 홍길동님, 입장을 환영합니다!
```

**예시 2 — 입장 불가**
```
회원권 상태를 입력하세요 (유효/만료): 만료
[회원권확인] 유효한 회원권이 없습니다
```

---

## 📊 채점 기준 (총 80점)

| 항목 | 배점 |
|------|------|
| 미들웨어 함수가 메인 기능보다 먼저 실행됨을 이해했는가 | 30점 |
| `== "유효"` 조건문으로 회원권 상태를 정확히 판별했는가 | 20점 |
| 반환값 `True/False` 를 받아 다음 동작을 결정할 수 있는가 | 20점 |
| 구문 오류 없이 실제로 실행되는가 | 10점 |

---

## 📁 제출 방법

| 항목 | 내용 |
|------|------|
| 파일명 | `02_mock_gym_이름.py` |
| Branch | 본인 이름 (영문) — 예) `HongGilDong` |
| Commit | `HongGilDong_02_mock_gym` |
| Repository | `https://github.com/seokssaem/bigdata-2026.git` |
