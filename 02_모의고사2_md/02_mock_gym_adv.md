# 모의고사 — 헬스장 입장 시스템 (심화)

> 응용SW 기초 기술 활용 · 미들웨어 파트 | 코딩 문제 1문항 · **80점**

---

## 📋 상황 설명

당신은 **헬스장 입장 시스템**을 만들고 있습니다.
이 시스템에는 세 가지 핵심 부분이 있습니다.

| 구분 | 함수명 | 역할 |
|------|--------|------|
| 미들웨어 ① | `check_membership(membership)` | 회원권 유효 여부를 확인하는 기능 |
| 미들웨어 ② | `check_locker(locker)` | 락커 번호가 유효한지 확인하는 기능 |
| 메인 시스템 | `enter_gym(name, locker)` | 입장을 처리하는 기능 (이미 작성되어 있음) |

---

## 🎯 수행 과제

아래 조건에 맞게 파이썬 코드를 완성하세요.

**① `check_membership(membership)` 함수를 작성하세요.**

- `"유효"` 이면 `"[회원권확인] 입장 가능합니다"` 출력 후 `True` 반환
- 그 외이면 `"[회원권확인] 유효한 회원권이 없습니다"` 출력 후 `False` 반환

**② `check_locker(locker)` 함수를 작성하세요.**

- 락커 번호는 **1번 이상 50번 이하**만 유효합니다.
- 유효하면 `"[락커확인] 사용 가능한 락커 번호입니다"` 출력 후 `True` 반환
- 유효하지 않으면 `"[락커확인] 락커 번호는 1번에서 50번 사이여야 합니다"` 출력 후 `False` 반환

**③ main 코드를 작성하세요.**

- 회원권 상태와 락커 번호를 각각 `input()` 으로 입력받습니다.
- **두 미들웨어가 모두 `True` 일 때만** 이름을 입력받고 `enter_gym()` 이 실행됩니다.
- ※ 락커 번호는 `int()` 로 변환하여 사용하세요.

---

## 💻 시작 코드

```python
def enter_gym(name, locker):
    print(f"💪 {name}님, {locker}번 락커를 사용하세요. 입장을 환영합니다!")


# ↓ 여기서부터 작성하세요

def check_membership(membership):
    pass  # 이 부분을 완성하세요


def check_locker(locker):
    pass  # 이 부분을 완성하세요


if __name__ == "__main__":
    pass  # 이 부분을 완성하세요
```

---

## 📌 실행 예시

**예시 1 — 정상 입장**
```
회원권 상태를 입력하세요 (유효/만료): 유효
락커 번호를 입력하세요: 23
[회원권확인] 입장 가능합니다
[락커확인] 사용 가능한 락커 번호입니다
이름을 입력하세요: 홍길동
💪 홍길동님, 23번 락커를 사용하세요. 입장을 환영합니다!
```

**예시 2 — 회원권 만료**
```
회원권 상태를 입력하세요 (유효/만료): 만료
락커 번호를 입력하세요: 5
[회원권확인] 유효한 회원권이 없습니다
```

**예시 3 — 락커 번호 오류**
```
회원권 상태를 입력하세요 (유효/만료): 유효
락커 번호를 입력하세요: 99
[회원권확인] 입장 가능합니다
[락커확인] 락커 번호는 1번에서 50번 사이여야 합니다
```

---

## 📊 채점 기준 (총 80점)

| 항목 | 배점 |
|------|------|
| 미들웨어 두 개가 메인 기능보다 먼저 실행됨을 이해했는가 | 20점 |
| `check_membership` — 회원권 상태 판별이 정확한가 | 20점 |
| `check_locker` — 범위 조건(1 ≤ n ≤ 50) 판별이 정확한가 | 20점 |
| 두 반환값 모두 `True` 일 때만 메인 함수가 실행되는가 | 10점 |
| 구문 오류 없이 실제로 실행되는가 | 10점 |

---

## 📁 제출 방법

| 항목 | 내용 |
|------|------|
| 파일명 | `02_mock_gym_adv_이름.py` |
| Branch | 본인 이름 (영문) — 예) `HongGilDong` |
| Commit | `HongGilDong_02_mock_gym_adv` |
| Repository | `https://github.com/seokssaem/bigdata-2026.git` |
