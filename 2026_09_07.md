# 학습일지 (daily-log.md)

## 작성자

* 이름: 홍은경

---

## 2026-09-07. 월
### 오늘 개별 공부한 것들
1) 빅분기 실기 시험 준비 계획
- 시험 일자 : 11월 28일 
- 참고 도서 : 빅데이터분기사 실기 with Python(길벗출판사)
- 필기시험 사전발표(9/18)까지 눈으로 1회독하기
- 합격시 실습을 통해 반복학습 예정
- 만약 불합격 한다면 ADsP(10월 21일 시험) 재도전 준비
---
2) 빅데이터분기사 실기 with Python(길벗출판사) 
- 읽기 첫째날 : 처음부터 ~ p. 67
- 잊고 있던 내용 및 학습이 미비한 내용 정리
    - enumerate() - 반복문에서 인덱스와 값을 함께 반환. 
    - zip() - 딕셔너리 변수에 있는 key와 value를 묶어서 반환.

    - 리스트에서 값 치환하기
    ```
    listbox=["감사", "행복", "사랑", "성공", "긍정", "변화", "성장", "희망"]
    listbox[5] = "웃음"
    print(listbox)
    ```

    - 문자열에서 값 치환하기
    ```
    str_data ="여러분의 합격을 응원합니다."
    str_data=str_data.replace("여러분", "당신")
    print(str_data)
    ```

    - 함수를 이용한 문자열에서 값 치환하기
    ```
    str_data ="여러분의 합격을 응원합니다."
    def replace(data):
        return str_data.replace("여러분", "당신")

    print(replace(str_data))
    ```
