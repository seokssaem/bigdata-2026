# 학습일지 (daily-log.md)

## 작성자

* 이름: 김시현

---

## 2026-08-03

### 오늘 개별 공부한 것들
- train.py: 
- 데이터 경로 설정 -> 숫자형 특성(피처, 컬럼)처리 파이프라인, 범주형(문자형) 특성 처리 파이프라인 -> 파이프라인 생성 후 학습 데이터로 학습 -> 보고용으로 핵심 자료만 정리 -> 학습된 파이프라인(전처리+모델) 파일로 저장
- streamlit_app.py: 고객 이탈 조기 경보
- 경로 설정하고 전처리 + 모델 복원한 후, `number_input` `selectbox` 위젯 만든 후 -> 버튼 생성하고 클릭을 하면 데이터 프레임 생성
- `predict_proba(row)` [비이탈 확률, 이탈 확률] 2차원 배열 반환